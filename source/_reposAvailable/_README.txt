Inspired by Nginx' philosophy:

- Repos are initially cloned here
   - As specified by repo_manifest.yml's root `init_clone_path` prop

- After cloned, it will be symlinked to repo_manifest.yml's symlink_path