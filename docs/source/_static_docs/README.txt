Anything locally here will be cloned into `../content/-`. Some may prefer to store static content within an outside repo (which is ok, too!)

(`-` because it's a tiny url slug, similarly seen in GitLab, opposed to something longer like `static/`)
TODO: Allow customizing `-` name via repo_manifest
