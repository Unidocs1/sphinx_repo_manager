# For all doc repos, including the main multi-repo doc (to be copied to other repo `requirements.txt`):
sphinx==7.3.7
sphinx_rtd_theme>=2.0.0
myst_parser>=0.18.0  # `recommonmark` successor
sphinx_tabs>=3.4.5
#breathe==4.35.0  # API DocGen: Provides bridge between Doxygen xml doc gen + sphinx (for C* languages)
#exhale==0.3.7  # Builds on top of breathe to provide a simpler way to generate API docs for C++ projects
#sphinx_csharp==0.1.8  # For DocGen (C#): syntax highlighting, auto-documentation
